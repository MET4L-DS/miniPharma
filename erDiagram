how to write ER diagram in text format to automatically generate as image
use snake case for table name and attributes for consistency sake
PK -> primary key
FK -> forign key
UK -> unique key
table_name {
        data_type attribute_name PK/FK/UK/blank "write comment inside inverted comma if required"
}

Example:
medicine {
        int med_id PK
        string name
        string brand "for local use generic"
        string type "Tablet, Syrup, Injection"
        bool prescription_required
        string hsn
        decimal(4, 2) gst
}

--- START WRITING FROM HERE ONWARDS ---
